{
  "name": "unbaact",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive granular equilibrium laboratory",
  "scripts": {
    "start": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test"
  },
  "dependencies": {
    "@dimforge/rapier2d-compat": "0.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^8.0.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
